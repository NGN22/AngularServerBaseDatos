import { Title } from '@angular/platform-browser';

export interface Game {
    id_contenido?: number,
    titulo?: string,
    fec_publicacion?: any,
    extension?: string,
    tipo?: any;
    image?: string,
};